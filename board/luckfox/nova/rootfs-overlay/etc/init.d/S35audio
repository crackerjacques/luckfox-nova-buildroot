#!/bin/sh
# Default mixer state - ALSA settings do not survive a reboot otherwise.
# The on-board mic is MIC8; tune the gain here if recordings clip.

case "$1" in
	start)
		amixer -q -c 0 sset 'MIC8' 100% cap 2>/dev/null
		;;
	stop)
		;;
	*)
		echo "Usage: $0 {start|stop}"
		;;
esac
